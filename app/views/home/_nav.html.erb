<nav class="top-bar" data-topbar role="navigation">

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <li id="third_stop" class="has-dropdown">
        <% if current_user.photo %>
          <a href="/users/<%=current_user.id%>">
            <%= image_tag current_user.photo.url(:thumb) %> <%=current_user.firstname %> <%= current_user.lastname %></a>
          <% else %>
            <a href="/users/<%=current_user.id%>">
            <%= current_user.firstname %> <%= current_user.lastname %></a>
          <% end %>
        <ul class="dropdown">
        <li>
          <a href="/users/<%=current_user.id%>">profile</a>
        </li>
          <li>
            <a href="/">fitmates</a>
          </li>
          <li>
            <a href="/conversations">messages</a>
          </li>
          <li>
            <%= link_to "logout", destroy_user_session_path, method: :delete %>
          </li>
        </ul>
      </li>
    </ul>

    <!-- Left Nav Section -->
    
    <ul class="left">
      <li id="first_stop"><a href="/conversations"><i class="fa fa-envelope-o"></i></a></li>
    </ul>

    <!-- Center Nav Section -->
    <div class="row">
      <div class="small-3 small-centered columns">
        <p class="fitmate-header"><a id="second_stop" href="/"><img src="<%= asset_path( 'fitmates_logo_text.png' ) %>" alt=""></a></p>
      </div>
    </div>

  </section>
</nav>